@model IEnumerable<Slide23.Models.AankoopOrder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hoeveelheid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Artikel.Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdentityUser.UserName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hoeveelheid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Artikel.ArtikelId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdentityUser.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AankoopOrderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AankoopOrderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AankoopOrderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
